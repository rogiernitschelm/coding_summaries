// Fastest

const array = [1, 2, 3];
array.constructor === Array; // Returns true

// Other forms of typechecking

typeof 'abc' // Returns string
[1, 2, 3] instanceof Array // Returns true
Array.isArray([1, 2, 3]) // Returns true
